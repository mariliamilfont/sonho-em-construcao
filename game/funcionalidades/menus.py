def draw_menu(screen, menu_surface):
    screen.blit(menu_surface, (0, 0))


def draw_win(screen, win_surface):
    screen.blit(win_surface, (0, 0))


def draw_dead(screen, dead_surface):
    screen.blit(dead_surface, (0, 0))
